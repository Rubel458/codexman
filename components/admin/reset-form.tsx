"use client"
import { FormEvent, useState } from "react"
import { useRouter, useSearchParams } from "next/navigation"
import { getCsrf } from "@/components/admin/csrf"
export function ResetForm(){const router=useRouter();const token=useSearchParams().get("token")||"";const [message,setMessage]=useState("");const[loading,setLoading]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget;const password=String(new FormData(form).get("password")||"");setLoading(true);const csrf=await getCsrf();const response=await fetch("/api/auth/reset-password",{method:"POST",headers:{"content-type":"application/json","x-csrf-token":csrf},body:JSON.stringify({token,password})});const json=await response.json().catch(()=>({}));setLoading(false);if(response.ok){setMessage("Password updated. Redirecting to login...");setTimeout(()=>router.push("/admin/login"),900)}else setMessage(json.error||"Reset failed.")}return <form onSubmit={submit} className="grid gap-4"><label className="grid gap-2 text-sm font-semibold">New password<input name="password" required type="password" minLength={12} className="rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 outline-none focus:ring-2 focus:ring-blue-200" /></label><button disabled={loading} className="brand-button cursor-pointer rounded-xl px-5 py-3 text-sm font-bold text-white disabled:opacity-60">{loading?"Updating...":"Update password"}</button>{message&&<p className="text-sm text-blue-700">{message}</p>}</form>}
