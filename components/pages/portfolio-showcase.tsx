"use client"
import Image from "next/image"
import Link from "next/link"
import { ArrowRight, ExternalLink, Plus } from "lucide-react"
import { useEffect, useMemo, useState } from "react"
type Project={slug:string;title:string;category:string;imageUrl:string;excerpt?:string|null;technologies?:unknown;liveUrl?:string|null;buttonLabel?:string|null;buttonUrl?:string|null}
function tech(value: unknown) { return Array.isArray(value) ? value.map(String) : [] }
export function PortfolioShowcase({ projects }: { projects: Project[] }) { const categories=useMemo(()=>["All",...Array.from(new Set(projects.map(p=>p.category)))],[projects]); const [active,setActive]=useState("All"); const [visible,setVisible]=useState(6); const items=active==="All"?projects:projects.filter(p=>p.category===active); useEffect(()=>setVisible(6),[active]); const shown=items.slice(0,visible); return <section className="py-20"><div className="mx-auto max-w-[1760px] px-6 lg:px-10"><div className="flex flex-wrap gap-3">{categories.map(category=><button key={category} onClick={()=>setActive(category)} className={`cursor-pointer rounded-full px-5 py-2.5 text-sm font-semibold transition duration-200 ${active===category?"bg-blue-700 text-white shadow-md":"border border-slate-200 bg-white text-muted-foreground hover:border-blue-200 hover:bg-blue-50 hover:text-blue-700"}`}>{category}</button>)}</div><div className="mt-10 grid gap-7 md:grid-cols-2 xl:grid-cols-3">{shown.map(project=><article key={project.slug} className="group overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm transition duration-300 hover:-translate-y-2 hover:border-blue-200 hover:shadow-xl"><Link href={`/portfolio/${project.slug}`} className="block cursor-pointer overflow-hidden"><Image src={project.imageUrl} alt={project.title} width={900} height={675} className="aspect-[4/3] w-full object-cover transition duration-700 group-hover:scale-105" /></Link><div className="p-6"><p className="text-xs font-bold uppercase tracking-[.18em] text-blue-700">{project.category}</p><Link href={`/portfolio/${project.slug}`} className="mt-2 block cursor-pointer"><h2 className="font-heading text-2xl font-semibold transition group-hover:text-blue-700">{project.title}</h2></Link><p className="mt-3 text-sm leading-relaxed text-muted-foreground">{project.excerpt || "A carefully designed digital project focused on speed, clarity and business results."}</p><div className="mt-5 flex flex-wrap gap-2">{tech(project.technologies).map(item=><span key={item} className="rounded-full bg-blue-50 px-3 py-1 text-xs font-semibold text-blue-700">{item}</span>)}</div><div className="mt-6 flex flex-wrap items-center gap-4"><Link href={project.buttonUrl||`/portfolio/${project.slug}`} className="inline-flex cursor-pointer items-center gap-2 text-sm font-bold text-blue-700">{project.buttonLabel||"Case study"}<ArrowRight className="size-4 transition group-hover:translate-x-1" /></Link>{project.liveUrl && <a href={project.liveUrl} className="inline-flex cursor-pointer items-center gap-2 text-sm font-bold text-sky-700">Live URL<ExternalLink className="size-4" /></a>}</div></div></article>)}</div>{visible<items.length&&<div className="mt-10 text-center"><button onClick={()=>setVisible(value=>value+6)} className="brand-button inline-flex cursor-pointer items-center gap-2 rounded-xl px-7 py-3.5 text-sm font-bold text-white"><Plus className="size-4" />Load more projects</button></div>}</div></section> }
