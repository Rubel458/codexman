"use client"
import Image from "next/image"
import { ExternalLink } from "lucide-react"
import { useMemo, useState } from "react"
type Demo={slug:string;title:string;category:string;imageUrl:string;previewUrl?:string|null;liveUrl?:string|null}
export function DemoShowcase({ demos }: { demos: Demo[] }) { const categories=useMemo(()=>["All",...Array.from(new Set(demos.map(d=>d.category)))],[demos]);const[active,setActive]=useState("All");const items=active==="All"?demos:demos.filter(d=>d.category===active);return <section className="py-20"><div className="mx-auto max-w-[1760px] px-6 lg:px-10"><div className="flex flex-wrap gap-3">{categories.map(category=><button key={category} onClick={()=>setActive(category)} className={`cursor-pointer rounded-full px-5 py-2.5 text-sm font-semibold transition duration-200 ${active===category?"bg-blue-700 text-white shadow-md":"border border-slate-200 bg-white text-muted-foreground hover:bg-blue-50 hover:text-blue-700"}`}>{category}</button>)}</div><div className="mt-10 grid gap-7 md:grid-cols-2 xl:grid-cols-3">{items.map(demo=><article key={demo.slug} className="group overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm transition duration-300 hover:-translate-y-2 hover:border-blue-200 hover:shadow-xl"><div className="overflow-hidden"><Image src={demo.imageUrl} alt={demo.title} width={900} height={675} className="aspect-[4/3] w-full object-cover transition duration-700 group-hover:scale-105" /></div><div className="p-6"><p className="text-xs font-bold uppercase tracking-[.18em] text-blue-700">{demo.category}</p><h2 className="mt-2 font-heading text-2xl font-semibold">{demo.title}</h2><div className="mt-5 flex flex-wrap gap-3">{demo.previewUrl&&<a href={demo.previewUrl} className="brand-button inline-flex cursor-pointer items-center gap-2 rounded-xl px-4 py-2.5 text-sm font-bold text-white">Preview<ExternalLink className="size-4" /></a>}{demo.liveUrl&&<a href={demo.liveUrl} className="inline-flex cursor-pointer items-center gap-2 rounded-xl border border-slate-200 px-4 py-2.5 text-sm font-bold text-blue-700 transition hover:bg-blue-50">Live demo<ExternalLink className="size-4" /></a>}</div></div></article>)}</div></div></section>}
