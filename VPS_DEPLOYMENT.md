# ITLABBD CMS VPS Deployment

Use the V15 procedure in:

```text
VPS_DEPLOYMENT_V15.md
```

The V15 guide replaces earlier cron, uploads and reverse-proxy instructions. It includes persistent storage outside release folders, trusted forwarded-header handling, Docker restart policies and a deterministic production build.
