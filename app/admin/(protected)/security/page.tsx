import { KeyRound, LockKeyhole, ShieldCheck } from "lucide-react"
export default function SecurityPage(){return <div><p className="text-xs font-semibold uppercase tracking-wider text-primary">Profile & Security</p><h1 className="mt-1 font-heading text-3xl font-bold">Security controls</h1><div className="mt-8 grid gap-4 md:grid-cols-3">{[{icon:LockKeyhole,title:"Bcrypt password validation",text:"Admin passwords are stored as bcrypt hashes and never as plain text."},{icon:KeyRound,title:"JWT session cookie",text:"Sessions use a signed HTTP-only cookie with SameSite protection."},{icon:ShieldCheck,title:"CSRF & rate limits",text:"Sensitive POST requests require CSRF tokens and authentication attempts are throttled."}].map(item=><article key={item.title} className="rounded-2xl border border-border bg-card p-6 shadow-sm"><item.icon className="size-7 text-primary"/><h2 className="mt-5 font-heading text-lg font-semibold">{item.title}</h2><p className="mt-3 text-sm leading-relaxed text-muted-foreground">{item.text}</p></article>)}</div><p className="mt-7 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">Password changes are handled through the secure forgot-password email flow. Keep SESSION_SECRET, DATABASE_URL and SMTP credentials outside version control.</p></div>}
